'use client';

import ResetPassword from '@/ui/features/ResetPassword';

const ResetPasswordPage = () => {
  return (
    <div className="relative w-full top-0 left-0 flex justify-center items-start min-h-full box-border">
      <ResetPassword />
    </div>
  );
};

export default ResetPasswordPage;
